Курстық жоба
Алдамжаров Ж

нұсқа 1

Тақырыбы: ытваотывоа


git init
git remote add origin https://github.com/Lifanna/checkers_test.git

git config --global user.name "nick at github"
git config --global user.email "email at github"

git pull origin master

git add .
git commit -m "first commit"

git push origin master
